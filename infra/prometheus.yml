# Prometheus scrape config for BuildSignal GTM.
# The API exposes funnel metrics at /metrics/prometheus (see docker-compose.yml).
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: buildsignal
    metrics_path: /metrics/prometheus
    static_configs:
      - targets: ["api:8000"]
