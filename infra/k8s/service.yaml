apiVersion: v1
kind: Service
metadata:
  name: speed-to-lead-agent
  labels:
    app.kubernetes.io/name: speed-to-lead-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: speed-to-lead-agent
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
